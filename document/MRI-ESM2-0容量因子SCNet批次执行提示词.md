```text
/goal
以 `scnet/goal.md` 为本次运行的唯一操作规范，在乌镇 199（SSH 主机别名 `scnet-wuzhen-199`，账号 `aclym5felp`）完成 MRI-ESM2-0 容量因子生产批次。

本批次的上游 BCSD 已全部完成，位于 `/work/home/aclym5felp/data/bcsd_outputs/MRI-ESM2-0/`。将这些输入直接视为可用；仅通过目录名和文件元数据核定实际 26 个国家名称及年份范围，不读取或校验 NetCDF 内容。

运行范围为上述 26 个国家、`ssp126`、`ssp245`、`ssp585`，以及 `solar`、`wind` 两类能源，共 26 × 3 × 2 = 156 个独立组合。先根据远程输入目录得到准确且去重的国家清单，并把实际清单、年份范围和组合总数写入本批次状态记录，再生成或补齐所需作业脚本。不得用占位国家名、推测名称或 `region=all` 提交生产作业。

检查远程仓库代码是否包含本批次所需生成器和最新实现。只有确有需要时，才按操作手册完成本地修改、测试、提交和推送，并在远程干净工作区 fast-forward 更新；不要在远程形成未回传的代码分叉。若现有代码已经满足要求，不做无关修改。

本目标明确授权：生成和部署作业脚本、执行 `sbatch`、持续监测，以及依据运行证据进行操作手册允许的有界重试和资源调整。该授权不包含修改科学算法、删除或覆盖既有数据、绕过完成判定，或在缺少明确依据时使用 `--overwrite`。

把 `scnet/models/MRI-ESM2-0/completion_status/progress.md` 作为本批次唯一的人类可读进度汇总。开始时用真实的 26 个国家替换其中的占位行；此后每完成一轮“监测、汇总、判断、执行”，即使状态没有变化，也要更新检查时间、各状态计数、156 个组合的进度矩阵、失败或重试证据、资源摘要和下一步动作。不要另建 `progress_summary.md` 代替它；其他结构化状态文件仍按操作手册维护。

持续定时执行同一控制循环，并在每轮安全补充可提交作业。账号暂时没有空槽、作业仍在排队或运行、或本轮没有状态变化，都不是结束条件，不要因此提前结束目标。只有在 156 个组合全部满足完成标准、状态记录彼此一致且没有遗留重复活动作业时，才将目标判定为完成。若遇到需要新增权限、凭据、科学算法修改或覆盖/删除数据才能解决的确定性阻塞，则停止相关变更，在 `progress.md` 中保留完整证据并向用户报告。

最终汇报实际国家清单、完成/失败/重试数量、关键 Job ID、远程代码版本、输出与日志位置、资源使用摘要，以及 `progress.md` 的最终状态。
```
