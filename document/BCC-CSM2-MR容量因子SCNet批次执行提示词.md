```text
/goal
以 `scnet/goal.md` 为本次运行的唯一操作规范，在以下四台乌镇服务器上协同完成 BCC-CSM2-MR 容量因子生产批次：

| SSH 主机 | 账号 | 区域 | 区域数 | CF 组合数 |
|---|---|---|---:|---:|
| `scnet-wuzhen-151` | `acw4hq2non` | Germany、Spain、Chile、South Africa、Japan、Sweden、France | 7 | 42 |
| `scnet-wuzhen-173` | `acrxpqssp4` | Greece、Romania、Austria、Italy、Turkey、India、México | 7 | 42 |
| `scnet-wuzhen-9259` | `acnqt6koeq` | Denmark、Ukraine、United Kingdom、Poland、Ireland、Australia | 6 | 36 |
| `scnet-wuzhen-9359` | `ac6xcutdaj` | Portugal、Netherlands、South Korea、Vietnam、Egypt、Brazil | 6 | 36 |

区域归属以本表为准；相较 `document/BCC-CSM2-MR四服务器BCSD运行前准备方案.md`，Greece 和 Romania 调整至 `scnet-wuzhen-173`，其余区域归属不变。同一区域的全部容量因子任务只能在表中指定服务器运行，不得跨账号拆分、迁移或重复提交。容量因子不区分 BCSD 的“单作业区域”和“三阶段区域”；每个 `energy × region × scenario` 都是一个独立 Slurm 作业。

本批次范围为 26 个区域、`ssp126`、`ssp245`、`ssp585`，以及 `solar`、`wind` 两类能源，即 26 × 3 × 2 = 156 个独立组合；四台分别负责 42、42、36、36 个组合。年份范围为 `2015-2060`。先在每台服务器通过其实际 BCSD 输入目录的目录名和文件元数据核定该账号负责的准确、去重区域名及年份范围，再生成或补齐该服务器的作业脚本。四台核定结果的并集必须与表中 26 个区域一致，且交集为空；不得使用占位区域名、推测名称或 `region=all` 提交生产作业。只做目录、软链、文件名、数量、大小和时间范围等元数据检查，不读取或校验 NetCDF 内容。

每台服务器运行前都必须准备代码仓库。仓库不存在时按 `scnet/goal.md` 克隆；仓库已存在时先检查分支、远程地址和工作区状态，仅在干净且未分叉的工作区执行 fast-forward-only 更新。确认生成器、S01E01、S01E02 和所需监控实现可用，并记录远程分支与提交号；四台必须运行同一代码版本。只有现有代码确实不满足本批次时，才在本地完成最小修改、测试、提交和推送，再让四台 fast-forward 更新；不得在远程直接形成未回传的代码分叉，也不得覆盖远程已有改动。

每台服务器运行前还必须准备该账号负责区域的 BCC-CSM2-MR 降尺度结果输入。先只读检查 `scnet-wuzhen-199` 上 `~/data/bcsd_outputs` 的现有软链层级、命名和解析方式，再在四台服务器上按相同结构建立软链适配层，使容量因子程序通过 `~/data/bcsd_outputs/BCC-CSM2-MR/<REGION>/BCC-CSM2-MR/*.nc` 访问该账号实际生成的 BCSD 结果。软链源必须是本表指定账号自身负责区域的现有 BCC-CSM2-MR 最终输出，不得把四台都指向 199，不得复制或移动整套数据。建立前检查目标路径：缺失时创建；已存在且解析正确时保留；已存在但类型或目标不符时停止并记录冲突，不得删除、覆盖或强制改链。逐台确认所有分配区域的链接可解析、目录可读，且四台输入区域合计恰好覆盖 26 个区域。

本目标明确授权在四台服务器上生成和部署作业脚本、执行 `sbatch`、持续监测，以及依据运行证据进行 `scnet/goal.md` 允许的有界重试和资源调整。该授权不包含修改科学算法、删除或覆盖既有数据、绕过完成判定，或在缺少明确依据时使用 `--overwrite`。

四个账号分别受“账号全部活动作业不超过 20”约束。每台每轮提交前均须取得该账号的 `$HOME/.bcsd_submit.lock`，统计该账号在 `squeue` 中所有项目的 `PENDING`、`RUNNING` 及其他非终态作业，并只提交 `max(0, 20 - active)` 个经精确去重的本机候选；每次 `sbatch` 前后重新计数。20 是逐账号硬上限，不是四台共享上限，也不能只统计本批次作业。某台无空槽不影响其他服务器在各自上限内继续运行，但不得把该台区域转移给其他账号。

把本地 `scnet/models/BCC-CSM2-MR/completion_status/progress.md` 作为整个四服务器批次唯一的人类可读进度汇总，不得为每台另建独立 `progress.md`，也不得用 `progress_summary.md` 代替它。文档必须同时保存：全局检查时间与 156 个组合的状态计数；四台服务器各自的账号、区域、代码版本、输入软链、活动作业数/20、已提交/运行/完成/失败/重试数量、关键 Job ID、资源摘要和下一步动作；以及包含服务器列的完整 156 组合进度矩阵。其他按服务器区分的结构化 CSV/JSON 状态文件仍按操作手册维护。每完成一轮“监测、汇总、判断、执行”，即使没有状态变化，也要原子更新同一个 `progress.md`；任何单台故障或暂时无槽都必须记录，但不能覆盖或丢失另外三台的既有进度。

持续对四台执行同一控制循环：用 `squeue`、`sacct`、退出码、少量日志尾部、`[CF_DONE]` 和预期非空输出文件进行判断，记录资源使用和有界重试证据，并在各账号空槽出现时安全补充其负责的未提交作业。不要因某账号没有空槽、作业仍在排队或运行、本轮没有状态变化，或仅部分服务器完成而提前结束。只有 156 个组合全部满足完成标准、四台结构化状态与 `progress.md` 一致、四个账号都没有本批次遗留或重复活动作业时，才进入汇总阶段。若遇到需要新增权限、凭据、科学算法修改或覆盖/删除数据才能解决的确定性阻塞，则停止相关变更，在 `progress.md` 中保留完整证据并向用户报告。

全部计算完成后，在 `scnet-wuzhen-199` 上只读检查 `~/data/cfs/CFs_of_solar` 和 `~/data/cfs/CFs_of_wind` 下其他已汇总模式的软链层级、相对/绝对链接形式和区域命名，然后为 BCC-CSM2-MR 按完全相同的结构创建汇总软链，使 199 上能够访问四个账号产出的全部 26 个区域、3 个 SSP 的 solar 和 wind 结果。链接源必须指向各区域实际生产服务器上的最终输出；不得复制结果文件，不得修改其他模式链接。若目标链接已存在，先核对其目标：正确则保留，冲突则停止并报告，不得覆盖。创建后逐链接检查可解析性，并按“能源 × 区域 × 情景”核对 156/156 个非空结果文件，无重复、无遗漏。

最终在不读取 NetCDF 内容的前提下，按文件元数据汇总并报告结果文件数量和存储大小：分别给出四台生产服务器的 solar、wind 和合计大小，给出全批次 solar、wind 和总大小，同时明确区分软链本身占用空间与其指向结果文件的实际大小，避免重复统计。最终汇报还须包含实际区域清单、各服务器和全局的完成/失败/重试数量、关键 Job ID、四台远程代码版本、输入软链、输出与日志位置、资源使用摘要、199 汇总软链位置，以及 `progress.md` 的最终状态。
```
