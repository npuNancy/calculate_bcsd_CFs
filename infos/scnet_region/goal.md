# Historical region CF guide

The original region-oriented SCNet material was archived here when the active
workflow moved to `global_bcsd` patchify. It is retained for reproducibility only;
new jobs must use `infos/scnet_patchify`.
