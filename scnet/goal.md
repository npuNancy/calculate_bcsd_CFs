# Patchify CF SCNet goal

Canonical operational contract: `infos/scnet_patchify/README.md`. One unit is
`model × scenario × patch × tech`; the generator only writes scripts and a
manifest. Scientific scripts run under `source /work/home/acbpgywfpz/miniconda3/bin/activate climate`.
CF output is station-only and is valid only with a matching JSON sidecar.
